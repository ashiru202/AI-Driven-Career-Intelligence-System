{
  "name": "career-skill-gap-extension",
  "version": "1.0.0",
  "private": true,
  "description": "Browser extension for Career Intelligence System",
  "type": "module",
  "scripts": {
    "clean": "node ./scripts/clean.mjs",
    "build": "node ./scripts/build.mjs",
    "build:watch": "node ./scripts/build.mjs --watch",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "jsdom": "^24.1.3",
    "vitest": "^2.1.5"
  }
}
